{
  "name": "apex-edge",
  "version": "3.0.0",
  "description": "NFL sharp-line finder — full rebuild with the complete factor set (market, usage, matchup, team efficiency, environment, schedule, injury/personnel) baked in.",
  "type": "module",
  "scripts": {
    "dry-run": "node scripts/dry-run.js",
    "backtest": "node scripts/backtest.js"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "papaparse": "^5.4.1"
  }
}
