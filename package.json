{
  "name": "apex-edge",
  "version": "4.1.0",
  "description": "NFL sharp-line finder — player-props-only rebuild: every prop graded to a model probability, cross-referenced against the market for real value, with fixed-band leg-disjoint parlay tiers and closing-line-value tracking.",
  "type": "module",
  "scripts": {
    "dry-run": "node scripts/dry-run.js",
    "backtest": "node scripts/backtest.js"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0",
    "papaparse": "^5.4.1"
  }
}
